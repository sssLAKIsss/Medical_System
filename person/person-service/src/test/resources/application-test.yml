logging:
  level:
    root: info
spring:
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
#  datasource:
#    driver-class-name: org.h2.Driver
#    url: jdbc:h2:mem:persons;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
#    username: postgres
#    password: postgres
  liquibase:
    parameters:
      textColumnType: TEXT
    contexts: nonprod
    change-log: classpath:db.changelog/db.changelog-master.yaml
    enabled: true

server:
  port: 8081

eureka:
  client:
    enabled: false
